# Database Configuration (Required)
# PostgreSQL connection string for Tiger Cloud/TimescaleDB
DATABASE_URL=postgresql://tsdbadmin:your_password@your_host:port/tsdb?sslmode=require

# Server Configuration
# Backend server port (defaults to 3001)
PORT=3001

# Frontend Configuration (for Vite)
# Frontend server port (defaults to 3000)
# VITE_PORT=3000

# Backend URL for Vite proxy (defaults to http://localhost:3001)
# Only needed if backend is on a different host/port during development
# VITE_BACKEND_URL=http://localhost:3001

# API URL for frontend (for cloud deployments)
# Leave empty for development (uses relative paths with Vite proxy)
# Set to full backend URL (e.g., https://api.example.com) for production/cloud
# VITE_API_URL=

# CORS Configuration (for cloud deployments)
# Frontend URL for CORS (defaults to http://localhost:3000)
# Set this to your frontend URL in cloud deployments
# FRONTEND_URL=http://localhost:3000
# CORS_ORIGIN=http://localhost:3000
